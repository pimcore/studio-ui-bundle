/**
 * @pimcore/studio-kit/components — the Studio component library
 * (grid, element-tree, form-kit, modals, upload, previews, relations, primitives …).
 * Opt-in subpath: importing it pulls the component graph, so the framework entry
 * (@pimcore/studio-kit) stays light. Rely on tree-shaking to keep only what you use.
 */
export * from '@sdk/components'
